/**
 * @author rik
 */
const modelsConfig = {
  // @todo allow this to be a function, so idAttributes like userId can be computed (by using model name)
  idAttribute: 'id',
  createdOnAttribute: 'createdAt',
  updatedOnAttribute: 'updatedAt'
};

export default modelsConfig;